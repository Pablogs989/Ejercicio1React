const Person = ({ name, surname, age }) => {
  return (
    <div>
      <span>{name} </span>
      <span>{surname} </span>
      <span>{age}</span>
      <p></p>
    </div>
  );
};
export default Person;